#include "main.h"

/**
 * _islower - checks if c is lower case
 * @c: The number to be checked.
 *
 * Return: 1 if lower else 0
 */
int _islower(int c)
{
	if (c > 96)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
